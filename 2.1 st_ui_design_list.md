以下係一份 Streamlit UI／版面設計元素嘅詳細清單，按功能分類，包含**項目（Item）**、**描述（Description）** 同**用法（How to Use）**。

---

## 1. 頁面設定 (Page Configuration)

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.set_page_config()` | 設定頁面標題、icon、layout（`centered` / `wide`）、sidebar 初始狀態 | `st.set_page_config(page_title="My App", layout="wide")` |
| `st.navigation()` | 多頁應用導航 | `pg = st.navigation([st.Page("page1.py"), st.Page("page2.py")])` |
| `st.Page()` | 定義單一頁面 | `st.Page("app.py", title="Home", icon="🏠")` |
| `st.logo()` | 在 sidebar 放 logo | `st.logo("logo.png")` |
| `st.html()` | 自訂 HTML | `st.html("<p>Hello</p>")` |

---

## 2. 版面 Layout / 容器 Containers

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.sidebar` | 左側持久面板，適合全局篩選、導航 | `st.sidebar.selectbox("Model", ["GPT-4", "Claude"])` |
| `st.columns()` | 水平分欄，可設定寬度比例、間距、垂直對齊 | `col1, col2 = st.columns(2)`；`left, right = st.columns([2, 1])` |
| `st.container()` | 通用分組容器，可加 border、height | `with st.container(border=True): st.write("Inside")` |
| `st.tabs()` | 分頁切換容器 | `tab1, tab2 = st.tabs(["Tab 1", "Tab 2"])` |
| `st.expander()` | 可摺疊展開面板，默認收起 | `with st.expander("See more"): st.write("Hidden content")` |
| `st.popover()` | 彈出氣泡框，點擊外部自動關閉 | `with st.popover("Filter"): st.checkbox("Show red")` |
| `st.dialog()` | 模態對話框，鎖定主頁面 | `@st.dialog("Title")` 裝飾器 |
| `st.form()` | 表單容器，一次過提交所有輸入 | `with st.form("my_form"): st.text_input("Name"); st.form_submit_button("Submit")` |
| `st.empty()` | 佔位符，之後可替換或清空 | `placeholder = st.empty(); placeholder.write("Later")` |
| `st.bottom()` | 頁面底部固定容器 | `with st.bottom(): st.write("Footer")` |
| `st.space()` | 加入垂直或水平間距 | `st.space(size="small")` |

---

## 3. 文字 / 排版 Text Elements

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.title()` | 大標題 | `st.title("The app title")` |
| `st.header()` | 標題 | `st.header("This is a header")` |
| `st.subheader()` | 副標題 | `st.subheader("This is a subheader")` |
| `st.markdown()` | Markdown 格式化文字 | `st.markdown("Hello **world**!")` |
| `st.caption()` | 小字說明 | `st.caption("Small caption text")` |
| `st.text()` | 固定寬度純文字 | `st.text("Hello world")` |
| `st.code()` | 代碼塊（可語法高亮） | `st.code("a = 1234")` |
| `st.latex()` | LaTeX 數學公式 | `st.latex(r"\int a x^2 \,dx")` |
| `st.divider()` | 水平分隔線 | `st.divider()` |
| `st.badge()` | 彩色標籤 | `st.badge("New")` |
| `st.write()` | 自動判斷類型顯示文字、資料、圖表 | `st.write("Hello", [1,2,3], df)` |
| `st.echo()` | 顯示代碼並執行 | `with st.echo(): st.write("Hello")` |

---

## 4. 資料顯示 Data Display

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.dataframe()` | 互動表格 | `st.dataframe(df)` |
| `st.data_editor()` | 可編輯表格 | `edited = st.data_editor(df)` |
| `st.table()` | 靜態表格 | `st.table(df)` |
| `st.metric()` | KPI 指標卡 | `st.metric("Revenue", "$12K", "8%")` |
| `st.json()` | JSON 顯示 | `st.json({"key": "value"})` |
| `st.pagination()` | 分頁控件 | `page = st.pagination(10)` |

---

## 5. 圖表 Charts

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.line_chart()` | 折線圖 | `st.line_chart(df)` |
| `st.area_chart()` | 面積圖 | `st.area_chart(df)` |
| `st.bar_chart()` | 條形圖 | `st.bar_chart(df)` |
| `st.scatter_chart()` | 散點圖 | `st.scatter_chart(df)` |
| `st.map()` | 地圖 | `st.map(df)` |
| `st.plotly_chart()` | Plotly 互動圖表 | `st.plotly_chart(fig)` |
| `st.altair_chart()` | Altair 圖表 | `st.altair_chart(chart)` |
| `st.pyplot()` | Matplotlib 圖表 | `st.pyplot(fig)` |
| `st.graphviz_chart()` | Graphviz 圖表 | `st.graphviz_chart(fig)` |
| `st.vega_lite_chart()` | Vega-Lite 圖表 | `st.vega_lite_chart(df, spec)` |

---

## 6. 輸入 / 控制 Widgets

### 按鈕類
| 項目 | 描述 | 用法 |
|------|------|------|
| `st.button()` | 按鈕 | `clicked = st.button("Click me")` |
| `st.download_button()` | 下載按鈕 | `st.download_button("Download", file)` |
| `st.form_submit_button()` | 表單提交按鈕 | `st.form_submit_button("Sign up")` |
| `st.link_button()` | 連結按鈕 | `st.link_button("Go", url)` |
| `st.menu_button()` | 選單按鈕 | `st.menu_button("Export", options=["CSV", "JSON"])` |
| `st.page_link()` | 頁面連結 | `st.page_link("app.py", label="Home")` |

### 選擇類
| 項目 | 描述 | 用法 |
|------|------|------|
| `st.checkbox()` | 勾選框 | `selected = st.checkbox("I agree")` |
| `st.radio()` | 單選按鈕 | `choice = st.radio("Pick one", ["cats", "dogs"])` |
| `st.selectbox()` | 下拉選單 | `choice = st.selectbox("Pick one", ["cats", "dogs"])` |
| `st.multiselect()` | 多選下拉 | `choices = st.multiselect("Buy", ["milk", "apples"])` |
| `st.select_slider()` | 選擇滑桿 | `size = st.select_slider("Size", ["S", "M", "L"])` |
| `st.toggle()` | 開關切換 | `activated = st.toggle("Activate")` |
| `st.color_picker()` | 顏色選擇器 | `color = st.color_picker("Pick a color")` |
| `st.feedback()` | 評分 / 情感按鈕組 | `st.feedback("stars")` |
| `st.pills()` | 膠囊按鈕選擇 | `st.pills("Tags", ["Sports", "AI"])` |
| `st.segmented_control()` | 分段按鈕選擇 | `st.segmented_control("Filter", ["Open", "Closed"])` |

### 數值類
| 項目 | 描述 | 用法 |
|------|------|------|
| `st.slider()` | 滑桿 | `number = st.slider("Pick a number", 0, 100)` |
| `st.number_input()` | 數字輸入 | `choice = st.number_input("Pick a number", 0, 10)` |

### 日期時間類
| 項目 | 描述 | 用法 |
|------|------|------|
| `st.date_input()` | 日期輸入 | `date = st.date_input("Your birthday")` |
| `st.time_input()` | 時間輸入 | `time = st.time_input("Meeting time")` |
| `st.datetime_input()` | 日期時間輸入 | `datetime = st.datetime_input("Schedule")` |

### 文字類
| 項目 | 描述 | 用法 |
|------|------|------|
| `st.text_input()` | 單行文字輸入 | `name = st.text_input("First name")` |
| `st.text_area()` | 多行文字輸入 | `text = st.text_area("Text to translate")` |
| `st.chat_input()` | 聊天輸入框 | `prompt = st.chat_input("Say something")` |

### 其他
| 項目 | 描述 | 用法 |
|------|------|------|
| `st.file_uploader()` | 檔案上傳 | `data = st.file_uploader("Upload a CSV")` |
| `st.camera_input()` | 相機拍照 | `image = st.camera_input("Take a picture")` |
| `st.audio_input()` | 錄音輸入 | `speech = st.audio_input("Record a message")` |

---

## 7. 媒體 Media

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.image()` | 顯示圖像（本地、URL、bytes） | `st.image("https://example.com/img.jpg")` |
| `st.audio()` | 音頻播放器 | `st.audio("audio.mp3")` |
| `st.video()` | 視頻播放器 | `st.video("video.mp4")` |
| `st.pdf()` | PDF 顯示 | `st.pdf("document.pdf")` |

---

## 8. 狀態 / 提示 Status & Progress

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.success()` | 成功提示（綠色） | `st.success("Done!")` |
| `st.info()` | 資訊提示（藍色） | `st.info("Info message")` |
| `st.warning()` | 警告提示（黃色） | `st.warning("Warning!")` |
| `st.error()` | 錯誤提示（紅色） | `st.error("Error!")` |
| `st.exception()` | 異常顯示 | `st.exception(e)` |
| `st.progress()` | 進度條 | `bar = st.progress(50); bar.progress(80)` |
| `st.spinner()` | 加載中旋轉器 | `with st.spinner("Loading..."): do_something()` |
| `st.status()` | 可展開狀態區 | `with st.status("Processing...") as s: ...` |
| `st.toast()` | 短暫彈出通知 | `st.toast("Saved!")` |
| `st.balloons()` | 氣球動畫 | `st.balloons()` |
| `st.snow()` | 雪花動畫 | `st.snow()` |
| `st.skeleton()` | 加載佔位動畫（新版） | `st.skeleton()` |

---

## 9. 控制流程 / 狀態管理

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.stop()` | 立即停止執行 | `st.stop()` |
| `st.rerun()` | 重新運行腳本 | `st.rerun()` |
| `st.switch_page()` | 切換頁面 | `st.switch_page("pages/profile.py")` |
| `st.session_state` | 跨 rerun 的狀態儲存 | `st.session_state["key"] = value` |
| `st.query_params` | URL 查詢參數 | `st.query_params["page"]` |
| `st.cache_data()` | 快取數據函數 | `@st.cache_data` |
| `st.cache_resource()` | 快取資源（如模型） | `@st.cache_resource` |
| `st.connection()` | 資料庫連接 | `conn = st.connection("mydb")` |
| `st.secrets` | 敏感資訊管理 | `st.secrets["api_key"]` |

---

## 10. 最新功能（2025–2026）

| 項目 | 描述 | 用法 |
|------|------|------|
| `st.dataframe` ButtonColumn | 表格內可點擊按鈕 | `st.dataframe(df, column_config={"action": st.column_config.ButtonColumn()})` |
| `st.skeleton()` | 加載佔位動畫 | `st.skeleton()` |
| `bind` 參數 | Widget 狀態同步 URL 查詢參數 | `st.slider("Value", bind="query_param")` |
| `on_change="ignore"` | 更新 widget 但不 rerun app | `st.selectbox("Choice", options, on_change="ignore")` |
| `wrap` 參數 | 控制水平佈局換行行為 | `st.columns(3, wrap=False)` |
| `st.segmented_control` | 分段按鈕選擇 | `st.segmented_control("Filter", ["Open", "Closed"])` |
| `st.pills` | 膠囊按鈕選擇 | `st.pills("Tags", ["Sports", "AI"])` |
| `st.feedback` | 評分按鈕組 | `st.feedback("stars")` |
| `filter_mode` | selectbox / multiselect 過濾模式 | `st.selectbox("Pick", options, filter_mode="contains")` |

---

## 常用排版組合範例

```python
import streamlit as st

st.set_page_config(layout="wide")

# Sidebar
with st.sidebar:
    model = st.selectbox("Model", ["GPT-4", "Claude", "Gemini"])
    temp = st.slider("Temperature", 0.0, 1.0, 0.7)

# Main layout
col1, col2 = st.columns([3, 1])

with col1:
    st.title("Dashboard")
    tab1, tab2 = st.tabs(["Overview", "Details"])
    with tab1:
        st.metric("Revenue", "$12K", "8%")
        st.line_chart(data)
    with tab2:
        with st.expander("Raw data"):
            st.dataframe(df)

with col2:
    st.image("logo.png")
    if st.button("Refresh"):
        st.rerun()
```

以上清單涵蓋咗 Streamlit 主要嘅 UI／版面設計元素，你可以按需要組合使用，砌出各種 dashboard 同 app。
